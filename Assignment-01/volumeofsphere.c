#include <stdio.h>
int main()
{
    // calculating volume of a sphere
    const float pi = 3.14;
    float radius = 4;
    printf("volume of a sphere is : %f", (4.0 / 3.0) * pi * (radius * radius * radius));
    return 0;
}