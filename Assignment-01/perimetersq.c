#include <stdio.h>
int main()
{
// calculating perimeter of a square
#define MY_CONSTANT 4
    float length;
    printf("enter length");
    scanf("%f", &length);
    printf("perimeter is : %f", 4 * length);
    return 0;
}