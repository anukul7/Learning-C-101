#include <stdio.h>
int main()
{
// calculating perimeter of a square
    float length;
    printf("enter length");
    scanf("%f", &length);
    printf("perimeter is : %f", 4 * length);
    return 0;
}