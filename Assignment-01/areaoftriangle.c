#include <stdio.h>
int main()
{
    // calculating area of a triangle
    float side;
    float base;
    printf("enter side");
    scanf("%f", &side);
    printf("enter base");
    scanf("%f", &base);
    printf("area is : %f", 1.0 / 2.0 * base * side);
}
