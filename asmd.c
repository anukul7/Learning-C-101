#include <stdio.h>
int main()
{
    int a = 4, b = 16;
    int sum = a + b;
    printf("sum is:%d", sum);
    int multiply = a * b;
    printf("multiplication is: %d", multiply);
    int divide = b / a;
    printf("division is : %d", divide);
    int subtract = b - a;
    printf("substraction is : %d", subtract);
    return 0;
}