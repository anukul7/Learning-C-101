#include <stdio.h>
int main()
{ 
    printf("sum,div,multiply,subract A and B");
    int a = 4, b = 16;
    int sum = a + b;
    int multiply = a * b;
    int divide = b / a;
    int subtract = b - a;
    return 0;
}